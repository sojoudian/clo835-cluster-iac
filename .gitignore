# Terraform
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
# (commit .terraform.lock.hcl — it pins provider versions for everyone)

# Keys / secrets — never commit these
*.pem
*.ppk
credentials

# OS
.DS_Store
